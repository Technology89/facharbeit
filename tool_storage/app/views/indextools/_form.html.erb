<%= render "shared/errors", object: @indextool %>
<%= form_for [@employee, @indextool], data: {abide: ''} do |f| %>
  <%= f.hidden_field :employee_id %>
  <%= f.hidden_field :ausgegeben_am %>
  <%= f.hidden_field :ausgegeben_von %>
  <div class="row">
    <div class="medium-3 columns">
      <%= label_tag :barcode %>
    </div>
    <div class="medium-9 columns">
      <%= number_field_tag :barcode, '', autofocus: true, required: '', pattern: 'barcode_field' %>
      <%= content_tag(:small, "Bitte geben sie einen gültigen Barcode ein", class: "error") %>
    </div>
  </div>
  <br/>  
  <div class="row">
    <div class="medium-12 columns">
      <%= f.submit "Ausgeben", class: 'button small radius' %>
    </div>
  </div>  
<% end %> 