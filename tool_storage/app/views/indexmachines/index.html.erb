<p id="notice"><%= notice %></p>

<h1>Maschinen von <%= @employee.Vorname %> <%= @employee.Nachname %></h1>

<table>
  <thead>
    <tr>
      <th>Ausgeliehen</th>
      <th>Ausgegeben am</th>
      <th>Machine</th>
      <th>Barcode</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @indexmachines.each do |indexmachine| %>
      <tr>
        <td><%= indexmachine.ausgeliehen %></td>
        <td><%= indexmachine.ausgegeben_am %></td>
        <td><%= @machines.find_by_id(indexmachine.machine_id).hersteller %> <%= @machines.find_by_id(indexmachine.machine_id).modell %></td>
        <td><%= @machines.find_by_id(indexmachine.machine_id).barcode %></td>
        <td><%= link_to 'Show', employee_indexmachine_path(@employee.id, indexmachine.id) %></td>
        <td><%= link_to 'Edit', edit_employee_indexmachine_path(@employee.id, indexmachine.id) %></td>
        <td><%= link_to 'Destroy', employee_indexmachine_path(@employee.id, indexmachine.id), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

